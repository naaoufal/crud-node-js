<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <!-- jQuery library -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <!-- Popper JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <!-- Latest compiled JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <title>Update Product</title>
</head>
<body>
    <div class="container"><br>
        <h2>Update Product :</h2><br>
        <form action="/updateProduct" method="post">
            <% for(var i=0; i<product.length; i++) { %>
            <input type="hidden" name="id" readonly="readonly" value="<%= product[i].idproduct %>">
            <div class="form-group">
                <label for="name">Enter Your Product Name :</label>
                <input type="text" value="<%= product[i].name %>" name="name" class="form-control" required>
            </div>
            <div class="form-group">
                <label for="price">Enter Your Product Price :</label>
                <input type="text" value="<%= product[i].price %>" name="price" class="form-control" required>
            </div>
            <%}%>
            <div class="form-group">
                <label for="category">Choose Your Category :</label>
                <select name="idcate" class="form-control">
                    <option>Select Your Category :</option>
                    <% for(var i=0; i<categories.length; i++) { %>
                        <option value="<%= categories[i].idcategory %>"><%= categories[i].idcategory %> - <%= categories[i].namecate %></option>
                    <% } %>
                </select>
            </div>
            <button class="btn btn-primary" type="submit">Update</button>
        </form>
    </div>
</body>
</html>